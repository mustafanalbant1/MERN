{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "npm install imagekit",
      "command": "npm",
      "args": ["install", "imagekit"],
      "options": {
        "cwd": "${workspaceFolder}/server"
      },
      "presentation": {
        "reveal": "silent"
      }
    }
  ]
}
